services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: zlagoda
      POSTGRES_USER: zlagoda
      POSTGRES_PASSWORD: zlagoda
    ports:
      - "5433:5432"
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql